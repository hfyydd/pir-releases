# pir-releases

Binary releases for `pir`.

This repository is used only as the public GitHub Releases endpoint for downloadable executables.
It does not publish the `pi-r` source repository.

Available release assets:
- macOS arm64
- Windows x64
